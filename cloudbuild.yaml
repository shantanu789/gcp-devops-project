options:
  logging: CLOUD_LOGGING_ONLY

steps:
  - name: 'docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/gcp-devops', '.' ]
  
  # - name: 'docker'
  #   args: ['push', 'gcr.io/$PROJECT_ID/gcp-devops' ]

images: 
  - 'gcr.io/$PROJECT_ID/gcp-devops'